from setuptools import setup

setup(
    name='fabric',
    version='1.0.0+alpha0',
    description='tav/pylibs fork of Fabric',
    packages=[
        str("fabric"),
        str("fabric.contrib"),
    ]
)
